name: 每日課程爬蟲

on:
  schedule:
    # 每天 UTC 01:00 執行 (台北時間 09:00)
    - cron: '0 1 * * *'
  workflow_dispatch:  # 允許手動觸發

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
    - name: 檢出代碼
      uses: actions/checkout@v3
      
    - name: 設定 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 安裝 Chrome 和 ChromeDriver
      run: |
        sudo apt-get update
        sudo apt-get install -y wget unzip
        
        # 取得最新的穩定版 Chrome 和對應的 ChromeDriver
        # 使用 Chrome for Testing 版本（確保版本匹配）
        CHROME_VERSION="131.0.6778.87"
        
        # 安裝特定版本的 Chrome
        wget -q "https://storage.googleapis.com/chrome-for-testing-public/${CHROME_VERSION}/linux64/chrome-linux64.zip"
        unzip -q chrome-linux64.zip
        sudo rm -rf /usr/bin/google-chrome /usr/bin/google-chrome-stable
        sudo mv chrome-linux64 /opt/chrome
        sudo ln -s /opt/chrome/chrome /usr/bin/google-chrome
        
        # 安裝對應版本的 ChromeDriver
        wget -q "https://storage.googleapis.com/chrome-for-testing-public/${CHROME_VERSION}/linux64/chromedriver-linux64.zip"
        unzip -q chromedriver-linux64.zip
        sudo rm -f /usr/local/bin/chromedriver
        sudo mv chromedriver-linux64/chromedriver /usr/local/bin/
        sudo chmod +x /usr/local/bin/chromedriver
        
        # 清理
        rm -rf chrome-linux64.zip chromedriver-linux64.zip chromedriver-linux64
        
        # 驗證版本
        google-chrome --version
        chromedriver --version
        
    - name: 安裝 Python 套件
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 讀取日期設定
      id: dates
      run: |
        if [ -f date_settings.json ]; then
          echo "日期設定檔存在"
        else
          echo "建立預設日期設定"
          cat > date_settings.json << EOF
        {
          "start_date": "$(date +%Y/%m/%d)",
          "end_date": "$(date -d '+30 days' +%Y/%m/%d)"
        }
        EOF
        fi
        
    - name: 執行爬蟲
      run: python scraper.py
      
    - name: 提交更新的資料
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add data.json
        git diff --quiet && git diff --staged --quiet || git commit -m "🤖 自動更新課程資料 $(date +'%Y-%m-%d %H:%M:%S')"
        
    - name: 推送變更
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
